
<!-- new note -->
<%= form_for [@budget, @note], :url => budget_notes_path, :html => { :class => 'well form-inline span12' } do |f| -%>
  
  <%= f.text_field :title, :class => 'text_field', :placeholder => 'title' %>

  <%= f.text_field :contents, :class => 'text_field', :placeholder => 'note...' %>

  <button class="btn btn-primary" type="submit">Add Note</button>

<% end -%>

<!-- notes list -->
<table class="table table-striped span12">
 <thead>
  <tr>
    <th>
      <%= t("activerecord.attributes.note.title", :default => t("activerecord.labels.title", :default => "Title")) %>
    </th>
    <th>Note</th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% @notes.each do |note| -%>
   <tr>
    <td><%= note.title %></td>
    <td><%= note.contents %></td>
    <td><%= note.created_at %></td>
    <td class="controls">
      <%= link_to "Destroy", "/budgets/%d/notes/%d"%[@budget.id, note.id], :class => 'danger', :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
    </td>
    </tr>
    <% end -%>
   </tbody>
</table>
