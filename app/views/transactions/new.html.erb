<section class="form span7">

    <%= form_for @transaction, :url => subline_transactions_path, :html => { :class => 'form-horizontal' } do |f| -%>
        <legend>New Transaction - <%= "%d.%d" % [@line.line_number, @subline.line_number]  %> <%= @subline.name -%></legend>
        <%= render :partial => "form", :locals => {:f => f} %>
    <% end -%>

</section>